const RoomScan: any = {};

Room.prototype.scan = function (room: Room): ScanRoom | boolean {

};

Room.prototype.runlogic = function runlogic(room: string): void {
  // console.log(Game.rooms[room].scan("hi"));
};
